(function () {
  'use strict';

  // Point-grant notifications are delivered through LINE push only.
  // Keep this module intentionally inert so opening PointsCard never shows
  // a second grant or reward popup for the same transaction.
})();